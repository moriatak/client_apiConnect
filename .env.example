# API Configuration
VITE_API_URL=https://qa.tak.co.il/campaignServer/connetToApi

# Security Settings
# הערה: האפליקציה מקבלת טוקן דרך URL parameter (?token=...)
# הטוכן חייב להיות בפורמט: payload.signature
# כאשר payload מכיל: sub, company_id, exp, iss, aud, nonce

# Skip Security Check (לפיתוח בלבד - אל תשתמש בייצור!)
# כדי לעקוף בדיקות iframe והורה, הגדר:
# VITE_SKIP_SECURITY_CHECK=true
# זה יאפשר גישה ישירה לאפליקציה ללא iframe
# ⚠️ חשוב: השאר את זה מושבת (false) או לא מוגדר בסביבת ייצור!
